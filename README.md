
  # Smart Medical Consultation App

  This is a code bundle for Smart Medical Consultation App. The original project is available at https://www.figma.com/design/SLryxpy8pz6Wi0pPiVmUsp/Smart-Medical-Consultation-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # ArtistianBrew
